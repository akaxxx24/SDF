---
plyfile: use "plyfile" library to load ply files

# We use mesh-to-sdf python package to transform mesh to SDF volume, which implements the strategy proposed by DeepSDF 
# Use following command to install mesh-to-sdf
pip install git+https://github.com/marian42/mesh_to_sdf.git